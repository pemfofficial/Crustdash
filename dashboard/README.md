# CrustDash dashboard

The Next.js app behind CrustDash. See the [main README](../README.md) for what it does, how it reads the game, and how to build a release.

```
npm install
npm run dev          # http://localhost:3000, reading ../data
npx tsc --noEmit     # type-check
npm run lint
```
